<div class="container">
    {{#each articles }}
                <div class="row">
                    <div class="col-sm-3">
                    </div>
                    <div class="col-sm-8" class="articles">
                        <h4>{{title}}</h4><br />
                        <a href={{link}}>Read Article</a><br /> 
                    </div>
                    <div class="col-sm-1">
                    </div>
                </div>
                  {{/each}}
            </div>


